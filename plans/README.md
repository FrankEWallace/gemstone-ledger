# Implementation Plans

Generated by the improve skill on 2026-07-07 (audit at commit `0e0bf99`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Repo verification commands (all plans use these): `npm ci`,
`npx tsc -p tsconfig.app.json --noEmit`, `npm run test`, `npm run lint`,
`npm run build`. CI (`.github/workflows/ci.yml`) runs lint + tsc + vitest +
playwright on npm.

**Standing rule for all plans**: local `supabase/migrations/` files have
previously diverged from the production database. Executors only WRITE
migration files; the operator verifies against prod and applies them. Never
apply a migration or deploy an edge function to a remote environment from a
plan.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Offline sync engine: truthful failures, serialized drain, dead-letter | P1 | M | — | DONE (reviewed+approved 2026-07-07; worktree branch `worktree-agent-a3e97a77c40afe1e5`, commit 1632108 — awaiting user merge; Dexie schema bumped to v3) |
| 002  | Invite-flow privilege escalations (app_metadata + role hierarchy) | P1 | M | — | DONE (reviewed+approved 2026-07-07; worktree branch `worktree-agent-ac33b70a4161ec697`, commit 603c4a8 — awaiting user merge; operator must follow deployment-order checklist in migration 026) |
| 003  | Edge-function tenant scoping + support-log PII | P1 | S | — | DONE (reviewed+approved 2026-07-07; worktree branch `worktree-agent-a40d0c48ca65fcfb5`, commit 9c5bca2 — awaiting user merge + edge-function deploy) |
| 004  | Idempotent offline replays + atomic inventory RPCs | P2 | M | 001 | TODO |
| 005  | Server-side report aggregation + bounded transactions fetch | P2 | L | — | DONE (reviewed+approved 2026-07-07; worktree branch `worktree-agent-ab7f9cf5b4a7d00b2`, commits 8f1ee6a/678d381/fe0a7fa — awaiting user merge; MIGRATION NUMBER COLLISION: this created migration 026, same number as plan 002's 026 — renumber one to 027 before merging; operator regenerates supabaseTypes after applying) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 004 requires 001: idempotent-replay semantics assume handlers throw on
  Supabase errors and the drain is serialized; 004 also edits the same handler
  blocks 001 touches.
- 002, 004, 005 each create a numbered migration (planned as 026, 027, 028).
  If they execute out of order, take the next free number and update the
  plan's internal references.
- 002 and 003 both deploy edge functions — coordinate operator deployment if
  they land together.

## Audit summary (2026-07-07, standard depth)

Full audit covered: correctness, security, performance, tests, tech debt,
dependencies, DX, docs, direction. Not audited: `src/components/ui/`
(vendored shadcn), `docs/` content accuracy in depth, the in-progress
design-token branch files (`src/lib/status.ts`, `StatusBadge.tsx`).

Findings vetted but NOT planned (candidates for a later run):

- `xlsx@0.18.5`: high advisories, no npm fix; single call site
  (`src/pages/reports/CustomerReportPage.tsx:333`). Fix: vendor CDN tarball or
  replace with CSV. Small, worth doing.
- `npm audit`: critical `vitest` advisory (dev-only) and high `ws` — both have
  `npm audit fix` paths.
- Demo mode guards reads but not most mutations (e.g.
  `customers.service.ts:34-48` falls through to real Supabase in demo mode).
- Storage policies on the site-documents bucket
  (`016_site_documents_bucket.sql:21-49`) allow any site member, including
  viewers, to upload/delete; no role predicate. Verify against prod first.
- `has_site_access`/`is_site_manager` (`005_rls_audit.sql`) reference
  `site_roles` while later code uses `user_site_roles` — verify which table
  prod actually resolves; reconcile.
- Optimistic message send can duplicate a bubble when the realtime echo beats
  the await (`src/pages/messages/MessagesPage.tsx:110-176`).
- Three lockfiles committed (bun.lock, bun.lockb, package-lock.json); CI uses
  npm — delete the bun lockfiles or switch wholesale.
- No root `CLAUDE.md`; README stale (migrations listed to 013 of 025, wrong
  env-example filename, missing test:e2e); duplicate env-example files.
- Sync-failure UX: failures only visible on SyncHistoryPage (001 adds
  dead-letter surfacing; a failure-time toast is still open).

## Findings considered and rejected

- "tsc never runs in CI": false — CI runs `npx tsc -p tsconfig.app.json
  --noEmit` (ci.yml:31). Only a local `typecheck` npm script is missing
  (cosmetic).
- "~100 latent type errors": stale — `tsc --noEmit` is clean at `0e0bf99`.
- REST provider branches as dead code to delete: deliberately NOT planned as
  deletion — the maintainer's recorded direction is a first-party backend
  ecosystem; the decision (commit with CI coverage vs shelve) belongs to the
  maintainer. Until decided, don't strip the branches.
- God-page refactors (6 pages > 890 lines): real debt, low leverage right now;
  revisit after the design-token/dashboard work in progress lands.
- `react-day-picker` v8 → v9 (+ date-fns v4): small blast radius but no
  urgency; opportunistic.
- Export libs in PWA precache, ComingSoon dead page, Supabase advisor lints:
  already fixed in PRs #4-#6 and migration 024.
- Anon key in client bundle, DiceBear avatars without upload, demo mode
  existing at all: by design.
