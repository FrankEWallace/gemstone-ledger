// Chart-container heights, in pixels, for Recharts numeric `height` props.
// Mirrors the h-chart-sm/md/lg CSS utilities in index.css — keep them in sync.
export const CHART_H = {
  sm: 180,
  md: 240,
  lg: 300,
} as const;
